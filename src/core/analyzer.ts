export interface NotablePoint { x: number; y: number; type: 'root' | 'intercept' | 'extremum' | 'intersect'; }export class MathAnalyzer {    /**     * O MILAGRE DA BISSEÇÃO: Encontra o zero exato da função cortando a distância pela metade.     * Chega à precisão microscópica (1e-7) em pouquíssimas iterações.     */    private static bisection(f: (x: number) => number, a: number, b: number, tol = 1e-7): number | null {        let fa = f(a), fb = f(b);        if (fa * fb > 0) return null; // Se não há mudança de sinal, não há raiz aqui        let mid = a;        for (let i = 0; i < 60; i++) {            mid = (a + b) / 2;            const fmid = f(mid);            if (Math.abs(fmid) < tol || (b - a) / 2 < tol) return mid;                        if (fa * fmid < 0) { b = mid; fb = fmid; }             else { a = mid; fa = fmid; }        }        return mid;    }    /**     * Caça Raízes (corta eixo X), Extremos (picos e vales) e Interseções com eixo Y     */    static getNotablePoints(f: (x: number) => number, xMin: number, xMax: number): NotablePoint[] {        const points: NotablePoint[] = [];        const steps = 150; // Resolução do radar (intervalos)        const dx = (xMax - xMin) / steps;        const h = 1e-5; // Microscópio para a derivada        // Derivada Numérica (Diferenças Finitas Centrais) - Calcula a inclinação da reta        const df = (x: number) => (f(x + h) - f(x - h)) / (2 * h);        let prevX = xMin;        let prevY = f(prevX);        let prevDy = df(prevX);        // 1. Interseção com o Eixo Y (Apenas avaliamos x = 0)        if (xMin <= 0 && xMax >= 0) {            const yInt = f(0);            if (!isNaN(yInt) && isFinite(yInt)) points.push({ x: 0, y: yInt, type: 'intercept' });        }        // Radar de Varredura        for (let i = 1; i <= steps; i++) {            const currX = xMin + i * dx;            const currY = f(currX);            const currDy = df(currX);            // 2. Caçador de Raízes (Mudança de sinal no Y)            if (prevY * currY <= 0) {                const rootX = this.bisection(f, prevX, currX);                if (rootX !== null) points.push({ x: rootX, y: 0, type: 'root' });            }            // 3. Caçador de Extremos (Mudança de sinal na Derivada/Inclinação)            if (prevDy * currDy <= 0) {                const extX = this.bisection(df, prevX, currX);                if (extX !== null) {                    const extY = f(extX);                    if (!isNaN(extY) && isFinite(extY)) points.push({ x: extX, y: extY, type: 'extremum' });                }            }            prevX = currX;            prevY = currY;            prevDy = currDy;        }        return this.filterDuplicates(points);    }    /**     * Caça as colisões entre DUAS funções diferentes subtraindo uma da outra!     */    static getIntersections(f: (x: number) => number, g: (x: number) => number, xMin: number, xMax: number): NotablePoint[] {        const h_func = (x: number) => f(x) - g(x); // A distância entre as duas vira uma nova função        const points: NotablePoint[] = [];        const steps = 150;        const dx = (xMax - xMin) / steps;        let prevX = xMin;        let prevY = h_func(prevX);        for (let i = 1; i <= steps; i++) {            const currX = xMin + i * dx;            const currY = h_func(currX);            // Se a distância zerou, elas se cruzaram!            if (prevY * currY <= 0) {                const rootX = this.bisection(h_func, prevX, currX);                if (rootX !== null) {                    const yVal = f(rootX);                     if (!isNaN(yVal) && isFinite(yVal)) points.push({ x: rootX, y: yVal, type: 'intersect' });                }            }            prevX = currX;            prevY = currY;        }        return this.filterDuplicates(points);    }    // Filtra pontos milimetricamente iguais para não sobrepor desenhos    private static filterDuplicates(points: NotablePoint[]): NotablePoint[] {        const unique: NotablePoint[] = [];        for (const p of points) {            const isDup = unique.some(u => Math.abs(u.x - p.x) < 1e-4 && Math.abs(u.y - p.y) < 1e-4);            if (!isDup) unique.push(p);        }        return unique;    }}